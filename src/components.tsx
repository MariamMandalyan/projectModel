import React, { useEffect, useState, useMemo } from 'react';
import {Text } from 'react-native';

const Components: React.FC = () => {
	return (
    <Text>Componentsner</Text>
	);
};
export default Components;